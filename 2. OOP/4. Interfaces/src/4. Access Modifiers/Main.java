public class Main {
    public static void main(String[] args) {
        var textBox = new TextBox();

        // now we can access field if we use protected keyword
        // new UIControl(true).isEnabled
    }
}
